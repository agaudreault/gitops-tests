apiVersion: batch/v1
kind: Job
metadata:
  name: job-timeout
spec:
  template:
    spec:
      containers:
      - name: wait
        image: busybox
        command: ['sh', '-c', 'sleep 10']
      restartPolicy: Never
  backoffLimit: 1